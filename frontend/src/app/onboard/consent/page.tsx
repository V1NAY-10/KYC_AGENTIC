import ConsentForm from '@/components/onboarding/ConsentForm';

export const metadata = {
  title: 'Digital Consent | KYC Onboarding',
  description: 'Digital consent for video KYC recording and data usage.',
};

export default function ConsentPage() {
  return <ConsentForm />;
}
