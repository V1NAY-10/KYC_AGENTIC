import SystemCheck from '@/components/onboarding/SystemCheck';

export const metadata = {
  title: 'System Check | KYC Onboarding',
  description: 'Ensure your device is ready for the AI video call.',
};

export default function SetupPage() {
  return <SystemCheck />;
}
